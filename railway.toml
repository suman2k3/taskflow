# Railway TOML Configuration for TaskFlow Monorepo

[build]
builder = "nixpacks"

[[services]]
name = "backend"
root = "backend"
startCommand = "npm start"

[[services]]
name = "frontend"
root = "frontend"
buildCommand = "npm run build"
startCommand = "npm run preview"
