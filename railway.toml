# Railway TOML Configuration for TaskFlow Fullstack Monorepo
# Backend serves both API and frontend static files

[build]
builder = "nixpacks"

[[services]]
name = "taskflow"
root = "backend"
buildCommand = "npm run build"
startCommand = "npm start"
